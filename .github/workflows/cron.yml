name: Call Next.js Cron API

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  call-cron-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call Next.js Cron API
        run: |
          curl -X POST ${{ secrets.CRON_URL }}/api/cron
